use_frameworks!

platform :ios, '8.0'

target 'LTPodTest1_Example' do
  pod 'LTPodTest1', :path => '../'

  target 'LTPodTest1_Tests' do
    inherit! :search_paths

    pod 'Specta'
    pod 'Expecta'
    pod 'FBSnapshotTestCase'
    pod 'Expecta+Snapshots'
  end
end
